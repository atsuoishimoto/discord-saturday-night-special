[project]
name = "saturday-night-special"
version = "0.1.0"
description = "Discord bot that bans accounts for spamming."
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.3.0",
    "python-dotenv>=1.0.0",
]
